# 需求评审原型扩展规范

## 1. 规范目的

本规范用于在同一个后台原型中持续叠加不同需求，并让评审人员能够：

- 查看不包含待评审改动的基线版本。
- 按需求名称切换并查看该需求带来的页面变化。
- 在页面标记与右侧改动说明之间快速定位。
- 通过完整 URL 分享指定需求、指定页面的评审现场。

当前暂不绑定需求 ID，以需求名称作为需求视图的唯一标识。

## 2. 核心定义

### 2.1 基线版本

“基线版本”表示当前已确认、可作为后续需求比较基础的原型。基线中不得出现尚未纳入基线的：

- 菜单、页面、字段、按钮或表格列。
- 业务规则、交互和校验。
- 模拟数据和状态。
- “本次新增”“本次调整”等改动标记。

### 2.2 需求视图

需求视图的展示结果为“基线版本 + 当前选择需求的改动”。不同待评审需求之间默认相互隔离，不应因为选择需求 A 而显示需求 B 的改动。

### 2.3 需求名称

需求名称用于选择器取值、代码判断和 URL 参数。在引入正式需求 ID 前，应遵守以下约束：

- 名称必须唯一，避免两个需求使用相同名称。
- 链接分享后不要随意改名；改名会导致旧链接无法还原需求视图。
- 页面文案与代码中的名称保持完全一致。
- “基线版本”为保留名称，不得用于具体需求。

## 3. 代码职责

| 位置 | 职责 |
| --- | --- |
| `core/requirement-view.js` | 登记需求、维护当前选择、生成改动说明、同步 URL、处理标记联动和面板拖拽 |
| `core/shell.js` | 根据活动菜单配置展示需求影响标记 |
| `activities/<activity>/index.js` | 按需求条件渲染业务页面、模拟数据、校验和交互 |
| `app.js` | 注册活动模块 |
| `styles.css` | 提供需求选择器、改动标记、说明面板和高亮样式 |

公共层只处理评审能力，不应写入某个活动的业务判断；业务差异应保留在对应活动模块内。

## 4. 新增需求的标准流程

### 4.1 登记需求

在 `core/requirement-view.js` 的需求配置中，以需求名称为键，按页面登记改动说明：

```js
const REQUIREMENTS = {
  "需求名称": {
    pages: {
      "activity-id/page": {
        title: "页面名称",
        items: [
          {
            target: "unique-change-point",
            type: "new",
            text: "新增某项配置"
          }
        ]
      }
    }
  }
};
```

每一条说明只描述一个可定位、可验证的改动点，避免把多个不相关改动合并成一句话。

### 4.2 隔离业务改动

在活动模块中通过需求名称判断是否展示改动：

```js
const showRequirement = isRequirementSelected("需求名称");
```

需要按需求隔离的内容包括：

- 页面结构和字段。
- 表格列、筛选条件及操作按钮。
- 示例数据和状态流转。
- 表单校验、提交逻辑及联动交互。
- 文案调整和默认值变化。

切换回基线版本后，上述内容必须恢复为基线状态，不能只隐藏标记而保留业务改动。

### 4.3 添加页面标记

页面中的改动位置使用统一标记：

```js
changeBadge("unique-change-point", "本次新增", "new")
```

标记与右侧说明通过 `target` 一一对应，并遵循以下规则：

- 同一页面内 `target` 必须唯一。
- 配置项的 `target` 必须与页面标记的 `data-change-point` 完全一致。
- 命名使用可读的英文短横线格式，例如 `passenger-task-type`。
- 点击页面标记应定位右侧说明；点击右侧说明应定位并高亮页面标记。
- 不允许登记没有页面落点的说明，也不允许存在没有说明的页面标记。

### 4.4 配置侧边栏标记

需求影响到某个菜单时，在对应菜单项配置需求名称：

```js
{
  label: "活动数据",
  route: "activity-id/data",
  requirementChanges: ["需求名称"]
}
```

只有选择该需求时，菜单才展示改动提示。一个菜单受多个需求影响时，可在数组中登记多个需求名称。

## 5. 改动类型规范

当前统一使用两类标记：

| 类型 | 展示文案 | 使用场景 |
| --- | --- | --- |
| `new` | 本次新增 | 新增菜单、页面、模块、字段、选项、按钮、表格列或状态 |
| `updated` | 本次调整 | 修改已有名称、展示规则、交互、校验、默认值或状态流转 |

判断标准是该元素在基线中是否存在，而不是开发工作量。删除类改动暂不单独设类型；出现明确需求后，应先补充统一视觉和交互，再在所有页面使用，避免自行扩展不同标记。

## 6. 右侧改动说明面板

- 面板只在具体需求视图下展示，基线版本不展示。
- 内容根据“当前需求 + 当前页面”生成。
- 面板宽度可拖动调整，建议范围为 260–560px。
- 宽度仅作为当前浏览器会话偏好保存，不写入分享 URL。
- 当前页面没有登记改动点时，面板不展示，且不得复用其他页面的说明。

## 7. URL 分享规范

当前需求名称写入查询参数 `requirement`，页面路由保留在哈希中：

```text
?requirement=需求名称#/activity-id/page
```

分享时复制浏览器完整地址。接收方打开后应还原：

- 已选择的需求名称。
- 当前活动及页面。
- 对应页面的改动标记和右侧说明。

需求名称由浏览器自动编码，不要手工拼接编码后的中文。无效或已删除的需求名称应回退到基线版本。

## 8. 基线合并与需求下线

需求确认需要纳入基线时，按以下顺序处理：

1. 将稳定的页面、数据、校验和交互变为默认逻辑。
2. 删除对应的需求条件分支。
3. 删除页面上的改动标记。
4. 删除侧边栏 `requirementChanges` 中的需求名称。
5. 删除 `core/requirement-view.js` 中该需求的登记信息。
6. 再次检查基线版本，确保功能仍完整且没有残留标记。

需求只是停止评审但尚未纳入基线时，不应直接合并到基线；可先从需求选择器下线，并保留相应业务分支以便后续恢复或清理。

## 9. 评审前验收清单

### 基线检查

- 基线不包含当前需求新增的页面元素、数据和交互。
- 基线不展示侧边栏改动提示、页面标记和右侧说明面板。
- 原有页面和操作仍可正常使用。

### 需求视图检查

- 需求选择器可以正确切换到目标需求。
- 所有需求改动都已展示，且不包含其他需求的改动。
- 每个页面标记与右侧说明一一对应，双向定位有效。
- `new` 与 `updated` 的类型使用符合本规范。
- 模拟数据足以演示新增状态和关键交互。

### 分享检查

- 刷新页面后仍能还原当前需求和路由。
- 在新的浏览器标签页打开完整 URL，展示结果一致。
- 无效需求名称能够安全回退到基线版本。

### 响应与交互检查

- 右侧面板拖动范围合理，不遮挡主要操作区域。
- 页面窄屏或横向滚动时，标记和目标内容仍可访问。
- 浏览器控制台没有错误，页面切换和表单操作无异常。
